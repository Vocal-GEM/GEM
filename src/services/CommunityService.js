const CommunityService = {
  getSuccessStories: async () => {
    // Return empty or mock data
    return { stories: [] };
  },
  submitSuccessStory: async (_data) => {
    // Simulate API call
    return { success: true };
  }
};

export default CommunityService;

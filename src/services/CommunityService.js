// Placeholder for CommunityService
const CommunityService = {
    getSuccessStories: async () => ({ stories: [] }),
    submitSuccessStory: async (story) => { return { success: true }; },
    upvoteStory: async (id) => { return { success: true }; }
};

export default CommunityService;

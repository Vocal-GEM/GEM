--- PitchWorklet.js
+++ PitchWorklet.js
@@ -1,3 +1,4 @@
+/* global currentTime, sampleRate */
 // Simple Worklet for audio capture
 // This delegates the actual pitch processing back to the main thread
 // or does lightweight processing if configured
